# MOHA-002: Migrate Database Schema to SQLite with Abstraction

> **Branch:** `MOHA-002`  
> **Status:** open  
> **Created:** 2026-04-10

## Original Request

> I want you to switch the database schema to use SQLite for now, I want the schema to be clear so in the future we can switch it to any database system we need create a task for that I want the task to be based on the MOHA-001 task an continious of it so what ever the developer complete will be used as the base for this task. First make sure that it can be done and then make sure that the description is complete

## Description

Building on the work completed in MOHA-001, migrate the existing database schema to use SQLite as the current storage engine while implementing a clean abstraction layer (e.g., repository pattern or ORM interface) that decouples business logic from the database technology. This ensures that switching to PostgreSQL, MySQL, or any other database in the future requires minimal code changes. The schema definitions should be explicit, well-documented, and database-agnostic in structure.

## Approach

Start by reviewing the output of MOHA-001 to understand the existing data models and any partial database setup already in place. Introduce SQLite as the active database engine via a connection/config abstraction, then define all schemas clearly using a migration or schema file format that can be ported to other databases. Wrap all data access behind a repository or data-access layer to prevent direct database coupling in business logic.

## Implementation Steps (7)

### Step 1: Review MOHA-001 Output and Existing Models

Examine all code, models, and data structures delivered in MOHA-001 to identify what entities, relationships, and data access patterns already exist. Document any assumptions or gaps before proceeding.

**Files:** `src/`, `README.md`

**Complexity:** low

---

### Step 2: Add SQLite Dependency and Database Config

Install the appropriate SQLite library for the project's language/framework and create a centralized database configuration file that specifies the connection string and engine. The config should support environment-based overrides so swapping databases later only requires a config change.

**Files:** `src/config/database.js`, `package.json`

**Complexity:** low

---

### Step 3: Define Explicit Schema with Migration Files

Create clear, version-controlled schema/migration files that define all tables, columns, types, constraints, and relationships derived from MOHA-001 models. Use standard SQL or a migration tool so schemas can be ported to any SQL-compatible database with minimal changes.

**Files:** `src/db/migrations/001_initial_schema.sql`, `src/db/schema.js`

**Complexity:** medium

---

### Step 4: Implement Database Abstraction Layer

Create a repository or data-access layer that wraps all SQLite queries behind well-defined interfaces (e.g., `UserRepository.findById(id)`). Business logic should never call the database driver directly, only through these abstractions.

**Files:** `src/db/repositories/`, `src/db/index.js`

**Complexity:** medium

---

### Step 5: Seed Initial Data and Validate Schema

Write a seed script that populates the SQLite database with minimal test data to validate the schema works correctly end-to-end. Run the seed and manually verify all tables and relationships are created as expected.

**Files:** `src/db/seed.js`

**Complexity:** low

---

### Step 6: Write Unit Tests for Repository Layer

Add unit or integration tests for each repository method using an in-memory SQLite instance to ensure data access logic is correct and isolated from business logic. Tests should cover basic CRUD operations for each entity.

**Files:** `tests/unit/repositories/`, `tests/integration/db/`

**Complexity:** medium

---

### Step 7: Document Schema and Abstraction Layer

Write clear documentation covering the schema design decisions, how to run migrations, how to add new repositories, and the steps required to swap SQLite for another database in the future. Add this to the project README or a dedicated docs file.

**Files:** `docs/database.md`, `README.md`

**Complexity:** low

---
_This file was auto-generated by DocContext AI. Update it as you progress._